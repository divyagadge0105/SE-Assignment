Edge Detection Viewer

This project contains a minimal working scaffold:
- Camera2 (YUV_420_888) -> JNI -> OpenCV C++ (Canny) -> returns RGBA -> OpenGL renderer displays texture
- Minimal TypeScript web viewer in /web

Follow instructions in the original ChatGPT message about installing NDK, OpenCV, and setting OpenCV_DIR in local.properties.
